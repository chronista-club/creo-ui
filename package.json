{
  "name": "creo-ui",
  "version": "0.0.1",
  "description": "creo-ui - Design System for Creo ecosystem",
  "private": true,
  "type": "module",
  "workspaces": ["packages/*", "apps/*"],
  "scripts": {
    "build": "bun run build:web && bun run build:swift && bun run build:rust",
    "build:web": "style-dictionary build --config transforms/config.web.js && bun scripts/build-web-components.mjs && bun run --cwd packages/web build:js",
    "build:swift": "style-dictionary build --config transforms/config.swift.js",
    "build:rust": "style-dictionary build --config transforms/config.rust.js",
    "typecheck": "tsc --noEmit",
    "lint": "biome check .",
    "format": "biome check . --write",
    "dev": "bun install --silent && bun run --cwd apps/site dev",
    "build:packages": "bun run --cwd packages/editor-host build && bun run --cwd packages/icons-web build && bun run --cwd packages/frame build && bun run --cwd packages/vision build && bun run --cwd packages/md-view build && bun run --cwd packages/layout build",
    "site:build": "bun run build:web && bun run build:packages && bun run --cwd apps/site build",
    "site:dev": "CREO_SITE_HTTP=1 bun run dev",
    "site:up": "bun run site:build && podman compose up -d --build --force-recreate",
    "site:down": "podman compose down",
    "site:deploy": "bun run site:build && bunx wrangler deploy",
    "gen:themes": "bun scripts/generate-themes.mjs",
    "test:colors": "bun test transforms/color-utils.test.js",
    "test:transforms": "bun test transforms/",
    "check:drift": "bun scripts/check-docs-drift.mjs",
    "check:tweak-vars": "bun scripts/check-tweak-vars.mjs",
    "fetch:design": "bun scripts/fetch-creo-ui-design.mjs",
    "diff:design": "bun scripts/diff-creo-ui-design.mjs"
  },
  "devDependencies": {
    "style-dictionary": "^4.0.0",
    "typescript": "^5.5.0",
    "@biomejs/biome": "^1.9.0"
  }
}
