node_modules/
dist/

# Phase 2a PoC — creoui-design release artifacts (download 先、 commit しない)
dist-creoui-design/
.DS_Store
*.log
.env
.env.local
bun.lockb
packages/*/dist/
packages/swift/.build/
packages/rust/target/
packages/rust/Cargo.lock
.idea/
.vscode/

# Generated token outputs for Swift / Rust ARE checked in — cargo build /
# swift build expect them to exist without first running `bun run build`.
# Web tokens live under `packages/web/dist/` which is gitignored (shipped
# only as npm publish artifact).
!packages/swift/Sources/Creoui/Generated/
!packages/rust/src/generated/
