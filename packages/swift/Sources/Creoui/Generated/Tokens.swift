//
// Tokens.swift
//
// Auto-generated by Style Dictionary from creoui tokens.
// Do not edit manually — edit tokens/**/*.json instead.
// Theme: mint-dark (Creo Design System default).
//

import SwiftUI
import CoreGraphics

public extension Color {
    static let colorSurfaceScrim = Color(red: 0.0000, green: 0.0000, blue: 0.0000) // Drawer / side sheet backdrop scrim — theme-agnostic 40% black overlay。 dark theme でも light theme でも統一して暗 backdrop を使う UX 慣習に従う。 中央 modal (dialog) はより重い注意を引くため `scrim-modal` (50%) を使う。
    static let colorSurfaceScrimModal = Color(red: 0.0000, green: 0.0000, blue: 0.0000) // Dialog (中央 modal) backdrop scrim — 50% black overlay。 drawer (40%) より強く content を後退させ、 user の注意を modal に集中させる。 dark/light theme 共通。
    static let colorBrandPrimary = Color(red: 0.3804, green: 0.7725, blue: 0.5804) // Brand primary (base)
    static let colorBrandPrimaryHover = Color(red: 0.4078, green: 0.8431, blue: 0.6314) // Brand primary — hover
    static let colorBrandPrimaryActive = Color(red: 0.3490, green: 0.7020, blue: 0.5294) // Brand primary — active / pressed
    static let colorBrandPrimarySubtle = Color(red: 0.0235, green: 0.1608, blue: 0.0980) // Brand primary — subtle tint surface
    static let colorBrandSecondary = Color(red: 0.6667, green: 0.5529, blue: 0.8706) // Brand secondary (base)
    static let colorBrandSecondaryHover = Color(red: 0.7333, green: 0.6039, blue: 0.9569) // Brand secondary — hover
    static let colorBrandSecondaryActive = Color(red: 0.6039, green: 0.4980, blue: 0.7843) // Brand secondary — active / pressed
    static let colorBrandSecondarySubtle = Color(red: 0.1451, green: 0.1098, blue: 0.2118) // Brand secondary — subtle tint surface
    static let colorSemanticSuccess = Color(red: 0.3569, green: 0.7137, blue: 0.3804) // Semantic success (base)
    static let colorSemanticSuccessHover = Color(red: 0.3961, green: 0.7843, blue: 0.4196) // Semantic success — hover
    static let colorSemanticSuccessActive = Color(red: 0.3176, green: 0.6471, blue: 0.3373) // Semantic success — active
    static let colorSemanticSuccessSubtle = Color(red: 0.0431, green: 0.1608, blue: 0.0549) // Semantic success — subtle surface
    static let colorSemanticSuccessText = Color(red: 0.5451, green: 0.8235, blue: 0.5529) // Semantic success — text on tinted bg
    static let colorSemanticWarning = Color(red: 0.8392, green: 0.6510, blue: 0.1804) // Semantic warning (base)
    static let colorSemanticWarningHover = Color(red: 0.9176, green: 0.7098, blue: 0.1961) // Semantic warning — hover
    static let colorSemanticWarningActive = Color(red: 0.7647, green: 0.5922, blue: 0.1647) // Semantic warning — active
    static let colorSemanticWarningSubtle = Color(red: 0.1843, green: 0.1176, blue: 0.0000) // Semantic warning — subtle surface
    static let colorSemanticWarningText = Color(red: 0.9255, green: 0.7882, blue: 0.5020) // Semantic warning — text on tinted bg
    static let colorSemanticError = Color(red: 0.9804, green: 0.4078, blue: 0.3882) // Semantic error (base)
    static let colorSemanticErrorHover = Color(red: 1.0000, green: 0.4549, blue: 0.4314) // Semantic error — hover
    static let colorSemanticErrorActive = Color(red: 0.8941, green: 0.3647, blue: 0.3451) // Semantic error — active
    static let colorSemanticErrorSubtle = Color(red: 0.2275, green: 0.0784, blue: 0.0706) // Semantic error — subtle surface
    static let colorSemanticErrorText = Color(red: 1.0000, green: 0.6824, blue: 0.6510) // Semantic error — text on tinted bg
    static let colorSemanticInfo = Color(red: 0.3098, green: 0.6588, blue: 0.8824) // Semantic info (base)
    static let colorSemanticInfoHover = Color(red: 0.3333, green: 0.7216, blue: 0.9725) // Semantic info — hover
    static let colorSemanticInfoActive = Color(red: 0.2824, green: 0.5922, blue: 0.7961) // Semantic info — active
    static let colorSemanticInfoSubtle = Color(red: 0.0235, green: 0.1412, blue: 0.2157) // Semantic info — subtle surface
    static let colorSemanticInfoText = Color(red: 0.5647, green: 0.8431, blue: 1.0000) // Semantic info — text on tinted bg
    static let colorSurfaceBgBase = Color(red: 0.0275, green: 0.0431, blue: 0.0784) // Root background — page canvas
    static let colorSurfaceBgSubtle = Color(red: 0.0471, green: 0.0706, blue: 0.1020) // Subtle background — panels, sections
    static let colorSurfaceBgEmphasis = Color(red: 0.1373, green: 0.1608, blue: 0.2000) // Emphasized background — highlighted regions
    static let colorSurfaceSurface = Color(red: 0.0667, green: 0.0863, blue: 0.1216) // Card / modal / elevated container surface
    static let colorSurfaceBorder = Color(red: 0.1569, green: 0.1804, blue: 0.2196) // Default border / divider
    static let colorSurfaceBorderSubtle = Color(red: 0.1137, green: 0.1333, blue: 0.1608) // Subtle border — quiet separators
    static let colorTextPrimary = Color(red: 0.9216, green: 0.9373, blue: 0.9608) // Primary foreground text
    static let colorTextSecondary = Color(red: 0.6667, green: 0.6824, blue: 0.7059) // Secondary text — supporting copy
    static let colorTextTertiary = Color(red: 0.4902, green: 0.5020, blue: 0.5255) // Tertiary text — captions, hints
    static let colorTextDisabled = Color(red: 0.3216, green: 0.3333, blue: 0.3569) // Disabled text
    static let colorTextInverse = Color(red: 0.0275, green: 0.0431, blue: 0.0784) // Inverse text — on dark surfaces
    static let colorShadowBase = Color(red: 0.0000, green: 0.0000, blue: 0.0000) // Shadow tint — default
    static let colorShadowStrong = Color(red: 0.0000, green: 0.0000, blue: 0.0000) // Shadow tint — strong
    static let colorFocusRingColor = Color(red: 0.4353, green: 0.9176, blue: 0.6824) // Focus ring color — outer 2px solid ring (mint-dark、 brand 同 hue + luminance UP で AAA contrast 確保)
    static let colorFocusRingHalo = Color(red: 0.4353, green: 0.9176, blue: 0.6824) // Focus halo — inner 4px subtle bg tint、 ring を 「包む glow」 として演出
}

public enum CreouiTokens {
    public static let depthFlat: CGFloat = 0 // Flat — z=0、 base plane (no elevation)
    public static let depthRaised: CGFloat = 4 // Raised — subtle hover lift / sidebar active item
    public static let depthElevated: CGFloat = 8 // Elevated — card / hero default forward (5-step rule の中央)
    public static let depthFloating: CGFloat = 16 // Floating — feature card hover / popover / dropdown
    public static let depthModal: CGFloat = 32 // Modal — overlay / dialog / drawer の前面 (最深 elevation)
    public static let editorModePanelFieldGap: CGFloat = 8 // Field 同士の縦間隔
    public static let editorModePanelGroupGap: CGFloat = 16 // Field group 間の縦間隔
    public static let editorModeRegionPadding: CGFloat = 12 // Region 内側の padding
    public static let editorModeRegionTopHeight: CGFloat = 44 // TOP region の固定高さ (Content を大きく隠さないコンパクトサイズ)
    public static let editorModeRegionBottomHeight: CGFloat = 44 // BOTTOM region の固定高さ
    public static let editorModeRegionLeftWidth: CGFloat = 240 // LEFT region の default 幅 (collapsible)
    public static let editorModeRegionRightWidth: CGFloat = 280 // RIGHT region の default 幅 (tool panel、左より広め)
    public static let editorModeSelectionOutlineWidth: CGFloat = 2 // Selection outline の太さ
    public static let editorModeSelectionOutlineOffset: CGFloat = 2 // 要素から outline までの offset (要素本体を遮らない)
    public static let focusRingWidth: CGFloat = 2 // Outer ring width — WCAG 2.4.11 minimum 2px contrast border、 thinner だと visible 不足
    public static let focusRingOffset: CGFloat = 2 // Outline offset — 要素境界から 2px 離す (要素を遮らず ring が呼吸)
    public static let focusRingHaloWidth: CGFloat = 4 // Inner halo width — outer ring の補完 subtle bg tint、 ring を 「包む glow」 として演出
    public static let framePerspectiveDefault: CGFloat = 1400 // Default perspective — page-level spatial frame (subtle 3D)
    public static let framePerspectiveShallow: CGFloat = 800 // Shallow — closer camera、 dramatic 3D (hero / playground)
    public static let framePerspectiveDeep: CGFloat = 2400 // Deep — farther camera、 subtle 3D (long-form content / reading)
    public static let layoutContainerXs: CGFloat = 480 // Extra-narrow column — single-line CTA / standalone form / compact dialog body。 mobile portrait の effective text width に近い、 minimal modal stage。
    public static let layoutContainerS: CGFloat = 640 // Narrow column — short form / single-line input collection / compact reading. iPhone 15 Pro Max landscape (932×430) や dense modal と整合。
    public static let layoutContainerM: CGFloat = 768 // Default reading-friendly — 60-80 char line for long-form text. iPad portrait の content viewport と整合。
    public static let layoutContainerL: CGFloat = 1024 // Dashboard / table-rich content — multi-column tables / data lists. iPad landscape / 13" laptop の effective viewport。
    public static let layoutContainerXl: CGFloat = 1280 // Hero / multi-column layout — landing / marketing / wide app shell。 14" laptop 以上 desktop の comfortable upper bound。
    public static let layoutGapTight: CGFloat = 4 // Tight gap — icon+label inline, chip group, closely-related siblings
    public static let layoutGapSibling: CGFloat = 18 // Default sibling gap — between stacked elements (list items, form fields)
    public static let layoutGapSection: CGFloat = 40 // Section-to-section gap — thematic block break (between content sections)
    public static let layoutGapPage: CGFloat = 64 // Page-level break — hero / footer separation, landing page major divisions
    public static let layoutGridColMinXs: CGFloat = 120 // auto-fit grid 最小 column 幅 (xs cells) — chip / avatar grid / icon-only tile。 dense 並びで多数表示が前提の最小単位。
    public static let layoutGridColMinS: CGFloat = 160 // auto-fit grid 最小 column 幅 (s cells) — image thumbnail / icon tile / chip group 等の 密 grid 向け。
    public static let layoutGridColMinM: CGFloat = 220 // auto-fit grid 最小 column 幅 (m cells) — card list / preview tile の標準サイズ、 readable summary を含む card に最適。
    public static let layoutGridColMinL: CGFloat = 280 // auto-fit grid 最小 column 幅 (l cells) — content card with headline + 1-2 line description + meta、 article preview。
    public static let layoutGridColMinXl: CGFloat = 320 // auto-fit grid 最小 column 幅 (xl cells) — feature card / hero tile、 multi-line description + CTA を含む rich card。
    public static let layoutTargetTap: CGFloat = 44 // Minimum tap target — Apple HIG accessibility requirement (modals, buttons on touch devices)
    public static let layoutTargetFocus: CGFloat = 32 // Minimum focus target — pointer devices / desktop-first (smaller than tap but still comfortable)
    public static let layoutTargetHit: CGFloat = 24 // Minimum hit target — dense UI (toolbars, inline actions), still WCAG-reasonable with hover affordance
    public static let marginXs: CGFloat = 8 // Extra-small margin — tight stack (paragraph spacing, inline gap)
    public static let marginS: CGFloat = 16 // Small margin — standard vertical rhythm between elements
    public static let marginM: CGFloat = 18 // Medium margin — component block separation (default、 spacing.m と揃える)
    public static let marginL: CGFloat = 40 // Large margin — section divider (between thematic blocks)
    public static let marginXl: CGFloat = 64 // Extra-large margin — major section or hero break (page-level breathing)
    public static let radiusNone: CGFloat = 0 // No rounding — explicit reset (special, outside 5 tier rule)
    public static let radiusXs: CGFloat = 4 // Extra-small radius — minimal rounding (chips, tags)
    public static let radiusS: CGFloat = 8 // Small radius — subtle rounding (buttons, inputs)
    public static let radiusM: CGFloat = 15 // Medium radius — cards, popovers (soft friendly feel)
    public static let radiusL: CGFloat = 22 // Large radius — modals, larger surfaces
    public static let radiusXl: CGFloat = 28 // Extra-large radius — hero surfaces
    public static let radiusFull: CGFloat = 9999 // Fully rounded — pill shapes and circles (special, outside 5 tier rule)
    public static let spacingXs: CGFloat = 4 // Extra-small spacing — 4px (base unit)
    public static let spacingS: CGFloat = 8 // Small spacing — 8px
    public static let spacingM: CGFloat = 18 // Medium spacing — 18px (default gap between UI elements)
    public static let spacingL: CGFloat = 24 // Large spacing — 24px
    public static let spacingXl: CGFloat = 32 // Extra-large spacing — 32px
    public static let typographyBodyLead: CGFloat = 20 // Lead body — opening paragraph / intro (larger, invites reading)
    public static let typographyBodyDefault: CGFloat = 16 // Default body — regular paragraph text (最もよく使う)
    public static let typographyBodyEmphasis: CGFloat = 16 // Emphasis body — default size with stronger weight (意味的な強調、size は default と同じ)
    public static let typographyBodyHelper: CGFloat = 14 // Helper text — form helper, status message, secondary info
    public static let typographyBodyCaption: CGFloat = 12 // Caption — image caption, footnote, meta info (smallest readable body)
    public static let typographyDisplayXs: CGFloat = 24 // Display xs — smallest heading (h4 / section title / card title)
    public static let typographyDisplayS: CGFloat = 32 // Display s — standard heading (h3 / article title / section headline)
    public static let typographyDisplayM: CGFloat = 44 // Display m — default hero / h2 (Creo aesthetic: 柔らかく強い存在感)
    public static let typographyDisplayL: CGFloat = 56 // Display l — page hero / h1
    public static let typographyDisplayXl: CGFloat = 72 // Display xl — landing mega headline / promotional hero
    public static let typographyIconXs: CGFloat = 16 // Tiny inline icon — text-flow に並ぶ 1em 相当の補助 mark / favicon / dense list の leading icon。
    public static let typographyIconS: CGFloat = 24 // Small icon — button / input / chip の leading mark、 toolbar action。 base 16px text と並ぶ comfortable 比率。
    public static let typographyIconM: CGFloat = 40 // Icon display — list item / inline 強調。 emoji や icon font の visual size、 typography size とは別 scale。
    public static let typographyIconL: CGFloat = 64 // Icon display — empty-state default / card header / feature illustration。
    public static let typographyIconXl: CGFloat = 96 // Icon display — empty-state large variant / hero illustration / onboarding。 page-level の visual anchor。
    public static let typographySizeXs: CGFloat = 12 // Caption / meta text
    public static let typographySizeS: CGFloat = 14 // Small body / helper text
    public static let typographySizeM: CGFloat = 16 // Default body text
    public static let typographySizeL: CGFloat = 18 // Large body / subheading
    public static let typographySizeXl: CGFloat = 20 // Largest body / small heading (h4)
    public static let typographyTitleHero: CGFloat = 72 // Hero title — landing page mega headline (promotional / marketing)
    public static let typographyTitlePage: CGFloat = 44 // Page title — h1 (article title, app screen heading)
    public static let typographyTitleSection: CGFloat = 32 // Section title — h2 (thematic block within a page)
    public static let typographyTitleSubsection: CGFloat = 24 // Subsection title — h3 (nested group within a section)
    public static let typographyTitleCard: CGFloat = 20 // Card title — h4 (inline block title, table header, menu label)

    public static let densityDefaultPaddingScale: Double = 1 // Default density — base spacing そのまま (4 mode の現状値)
    public static let densityDefaultGapScale: Double = 1 // Default density — base gap そのまま
    public static let densityDefaultMinHeightScale: Double = 1 // Default density — base target そのまま
    public static let densityComfortablePaddingScale: Double = 1.25 // Comfortable density — base × 1.25、 広い呼吸 (reading mode / hero / onboarding)
    public static let densityComfortableGapScale: Double = 1.25 // Comfortable density — gap も × 1.25 で element 間に余裕
    public static let densityComfortableMinHeightScale: Double = 1.1 // Comfortable density — target × 1.1 (tap も focus も少し大きく)
    public static let densityCompactPaddingScale: Double = 0.85 // Compact density — base × 0.85、 dense interface (data table / dashboard / dev tool)
    public static let densityCompactGapScale: Double = 0.85 // Compact density — gap も × 0.85
    public static let densityCompactMinHeightScale: Double = 0.95 // Compact density — target × 0.95 (tap >= 44px は維持、 focus は少し縮)
    public static let densityCozyPaddingScale: Double = 0.7 // Cozy density — base × 0.7、 最も dense (terminal mode / log viewer / max info-density)
    public static let densityCozyGapScale: Double = 0.7 // Cozy density — gap も × 0.7
    public static let densityCozyMinHeightScale: Double = 0.85 // Cozy density — target × 0.85 (a11y minimum 24px は超える)
    public static let editorModeOverlayBackdropOpacity: Double = 0 // Editor Layer 最背面の opacity。デフォルト 0 (完全透過) で Content Layer を遮らない
    public static let editorModeRegionBgOpacity: Double = 0.92 // Region 背景の 不透明度 (0.0-1.0)。Content を完全に隠さず薄く透かす
    public static let typographyLineHeightTight: Double = 1.25 // Tight line-height — headings
    public static let typographyLineHeightNormal: Double = 1.5 // Normal line-height — default body
    public static let typographyLineHeightRelaxed: Double = 1.75 // Relaxed line-height — long-form text, CJK languages
    public static let typographyWeightRegular: Double = 400 // Regular body weight
    public static let typographyWeightMedium: Double = 500 // Medium emphasis
    public static let typographyWeightSemibold: Double = 600 // Semi-bold — subheadings, buttons
    public static let typographyWeightBold: Double = 700 // Bold — headings

    public static let colorGradientHero: String = "linear-gradient(135deg, oklch(0.2 0.04 160) 0%, oklch(0.18 0.03 175) 50%, oklch(0.22 0.05 165) 100%)" // Hero gradient — marketing / splash surfaces
    public static let editorModeAxisGlobal: String = "var(--color-brand-secondary)" // TOP 領域 accent — global tools / 視線の起点 (active theme の brand secondary に追従)
    public static let editorModeAxisUtility: String = "var(--color-text-tertiary)" // BOTTOM 領域 accent — ローカル utility (active theme の text tertiary)
    public static let editorModeAxisPast: String = "var(--color-semantic-info)" // LEFT 領域 accent — 時系列過去 / 参照 / 元ソース (active theme の semantic info)
    public static let editorModeAxisFuture: String = "var(--color-brand-primary)" // RIGHT 領域 accent — 時系列未来 / ツール / 生成 (active theme の brand primary)
    public static let editorModePanelFieldLabel: String = "var(--color-text-secondary)" // Field label の text 色 (active theme の text secondary)
    public static let editorModePanelFieldValue: String = "var(--color-text-primary)" // Field value の text 色 (active theme の text primary)
    public static let editorModePanelSeparator: String = "var(--color-surface-border-subtle)" // Field group の区切り線 (active theme の border subtle)
    public static let editorModeRegionBgColor: String = "var(--color-surface-bg-base)" // Region 背景の base RGB (active theme に追従)
    public static let editorModeRegionBorder: String = "var(--color-surface-border)" // Region の輪郭色 (active theme に追従)
    public static let editorModeSelectionOutlineHover: String = "var(--color-brand-primary-subtle)" // Editor mode 中の hover outline (ghost highlight、Content を触らず上に描画)
    public static let editorModeSelectionOutlineActive: String = "var(--color-brand-primary)" // 選択中の要素 outline (active theme の brand primary)
    public static let focusRingStyle: String = "solid" // Outline style — solid のみ採用 (dashed は精度に欠ける、 double は thicker 設計)
    public static let motionDurationInstant: String = "80ms" // Instant — micro feedback (button click flash 等)
    public static let motionDurationFast: String = "160ms" // Fast — small UI transitions (tooltip / hover)
    public static let motionDurationNormal: String = "220ms" // Normal — default transition (card lift / nav switch、 5-step 中央)
    public static let motionDurationSlow: String = "320ms" // Slow — Frame morph / page transition (perceptible motion)
    public static let motionDurationLazy: String = "480ms" // Lazy — large layout shift / hero reveal (deliberate)
    public static let motionEasingLinear: String = "linear" // Linear — constant velocity (progress bars 等の cumulative motion)
    public static let motionEasingIn: String = "cubic-bezier(0.4, 0, 1, 1)" // Ease-in — slow start, fast end (退場 animation)
    public static let motionEasingOut: String = "cubic-bezier(0, 0, 0.2, 1)" // Ease-out — fast start, slow end (登場 animation、 Material 3 default)
    public static let motionEasingInOut: String = "cubic-bezier(0.4, 0, 0.2, 1)" // Ease-in-out — smooth both ends (state change、 standard default)
    public static let motionEasingSpring: String = "cubic-bezier(0.2, 0.8, 0.2, 1)" // Spring — Creo brand bouncy curve (3D Frame morph / lively interactions)
    public static let motionMappingHoverDuration: String = "160ms" // Hover transition duration — fast (160ms)、 small surface motion で 「変化が現れる」 感
    public static let motionMappingHoverEasing: String = "cubic-bezier(0, 0, 0.2, 1)" // Hover transition easing — ease-out で 「登場 → 落ち着く」 (Material 3 default)
    public static let motionMappingPressDuration: String = "80ms" // Press transition duration — instant (80ms) で micro feedback、 押下の即時感
    public static let motionMappingPressEasing: String = "cubic-bezier(0.4, 0, 1, 1)" // Press transition easing — ease-in で 「押し込み」 (急停止)
    public static let motionMappingToggleDuration: String = "220ms" // Toggle transition duration — normal (220ms) で滑らかな state change
    public static let motionMappingToggleEasing: String = "cubic-bezier(0.4, 0, 0.2, 1)" // Toggle transition easing — in-out で両端滑らか (radio / checkbox / switch / segmented)
    public static let motionMappingFocusRingDuration: String = "80ms" // Focus ring transition duration — instant (80ms) で keyboard tab に即応
    public static let motionMappingFocusRingEasing: String = "cubic-bezier(0, 0, 0.2, 1)" // Focus ring transition easing — ease-out で「ring が現れる」
    public static let motionMappingDropdownDuration: String = "160ms" // Dropdown / popover / tooltip duration — fast (160ms) で軽快な登場
    public static let motionMappingDropdownEasing: String = "cubic-bezier(0, 0, 0.2, 1)" // Dropdown easing — ease-out で 「ふわっと出る」
    public static let motionMappingModalEnterDuration: String = "320ms" // Modal entry duration — slow (320ms) で「落ち着いた登場」 (modal / drawer / dialog)
    public static let motionMappingModalEnterEasing: String = "cubic-bezier(0, 0, 0.2, 1)" // Modal entry easing — ease-out で fast → slow (前面に現れて止まる)
    public static let motionMappingModalExitDuration: String = "220ms" // Modal exit duration — normal (220ms) で「素早く消える」
    public static let motionMappingModalExitEasing: String = "cubic-bezier(0.4, 0, 1, 1)" // Modal exit easing — ease-in で slow → fast (退場、 引っ込む)
    public static let motionMappingPageTransitionDuration: String = "220ms" // Page route transition duration — normal (220ms) で滑らかな移動
    public static let motionMappingPageTransitionEasing: String = "cubic-bezier(0.4, 0, 0.2, 1)" // Page transition easing — in-out で両端滑らか (route 変化)
    public static let motionMappingFrameMorphDuration: String = "320ms" // Frame system morph duration — slow (320ms)、 Creo brand identity の「呼吸」
    public static let motionMappingFrameMorphEasing: String = "cubic-bezier(0.2, 0.8, 0.2, 1)" // Frame morph easing — spring で Creo brand bouncy (3D depth metaphor articulate)
    public static let motionMappingSkeletonShimmerDuration: String = "480ms" // Skeleton shimmer duration — lazy (480ms) で loading の cumulative motion
    public static let motionMappingSkeletonShimmerEasing: String = "linear" // Skeleton shimmer easing — linear で「途切れない流れ」 (loading は時間流動)
    public static let motionMappingProgressIndeterminateDuration: String = "480ms" // Progress indeterminate bar duration — lazy (480ms) で end-less cumulative motion
    public static let motionMappingProgressIndeterminateEasing: String = "linear" // Progress indeterminate easing — linear で constant velocity (途中 progress 表現)
    public static let shadowNone: String = "none" // No shadow
    public static let shadowS: String = "0 1px 2px 0 rgba(32, 36, 46, 0.06)" // Small elevation — quiet lift for subtle layers
    public static let shadowM: String = "0 4px 6px -1px rgba(32, 36, 46, 0.08), 0 2px 4px -2px rgba(32, 36, 46, 0.06)" // Medium elevation — cards, dropdowns
    public static let shadowL: String = "0 10px 15px -3px rgba(32, 36, 46, 0.10), 0 4px 6px -4px rgba(32, 36, 46, 0.08)" // Large elevation — popovers, overlays
    public static let shadowXl: String = "0 20px 25px -5px rgba(32, 36, 46, 0.12), 0 8px 10px -6px rgba(32, 36, 46, 0.08)" // Extra-large elevation — modals, focal surfaces
    public static let typographyFamilySans: String = "'Creo Sans', -apple-system, BlinkMacSystemFont, 'Segoe UI Variable', 'Segoe UI', 'Yu Gothic UI', 'Meiryo UI', Roboto, 'Noto Sans JP', sans-serif" // Default UI sans-serif family (multi-language: EN/JA/KO, multi-platform: macOS/Win/Linux)
    public static let typographyFamilyMono: String = "'JetBrainsMono Nerd Font', 'FiraCode Nerd Font', 'CaskaydiaCove Nerd Font', 'Iosevka Nerd Font', '0xProto Nerd Font', 'IBM Plex Mono', 'JetBrains Mono', 'SF Mono', 'Cascadia Code', 'Cascadia Mono', 'Fira Code', 'Consolas', 'Yu Gothic UI', monospace" // Default monospace family for code blocks, tokens, IDs. Top 5 Nerd Fonts (JetBrainsMono / FiraCode / Cascadia / Iosevka / 0xProto) を primary fallback、IBM Plex Mono を corporate fallback。Nerd Font が install 済なら icon glyph も同 stack で使える
    public static let typographyFamilyMonoLegible: String = "'AtkinsonHyperlegibleMono Nerd Font', 'Atkinson Hyperlegible Mono', 'Hack Nerd Font', 'Hack', 'JetBrainsMono Nerd Font', 'JetBrains Mono', monospace" // Mono variant for accessibility / high legibility. Atkinson Hyperlegible Mono (Braille Institute) + Hack を主軸。低視力 / 小サイズ / long session で読みやすさ最優先
    public static let typographyFamilyMonoRetro: String = "'DepartureMono Nerd Font', 'Departure Mono', 'Gohu Nerd Font', 'GohuFont', '3270 Nerd Font', '3270 Medium', 'Terminus Nerd Font', 'Terminus', 'JetBrainsMono Nerd Font', monospace" // Mono variant for retro / pixel / lo-fi aesthetic. Departure Mono / GohuFont / 3270 / Terminus の bitmap-like / pixel-perfect な font 群。display heading や theme アクセントで使う
    public static let typographyFamilyMonoCorporate: String = "'IBM Plex Mono Nerd Font', 'IBM Plex Mono', 'AdwaitaMono Nerd Font', 'Adwaita Mono', 'JetBrainsMono Nerd Font', 'JetBrains Mono', monospace" // Mono variant for corporate / professional tone. IBM Plex Mono が主軸 (Plex Sans/Serif と family 統一可)、Adwaita Mono が GNOME/Linux native flavor
    public static let typographyFamilyMonoDisplay: String = "'ShareTechMono Nerd Font', 'Share Tech Mono', 'VictorMono Nerd Font', 'Victor Mono', 'JetBrainsMono Nerd Font', 'JetBrains Mono', monospace" // Mono variant for display / heading / cyberpunk aesthetic. Share Tech Mono (CRT terminal feel) や Victor Mono (cursive italic 強調) が主軸。banner / hero / accent 用
    public static let typographyFamilyDisplay: String = "'Creo Sans', -apple-system, BlinkMacSystemFont, 'Segoe UI Variable', 'Segoe UI', 'Yu Gothic UI', 'Noto Sans JP', sans-serif" // Display family — heroes, large marketing headlines (multi-platform)
    public static let typographyFamilyIcon: String = "'Symbols Nerd Font Mono', 'Symbols Nerd Font', 'JetBrainsMono Nerd Font', 'FiraCode Nerd Font', 'CaskaydiaCove Nerd Font', 'Iosevka Nerd Font', '0xProto Nerd Font', 'Apple Color Emoji', 'Segoe UI Emoji', 'Segoe UI Symbol', 'Noto Color Emoji', sans-serif" // Icon family — Nerd Fonts (~10k アイコン: devicons / octicons / font-awesome / material) + OS native emoji fallback。Nerd Font 未 install 環境では emoji / 普通文字に degrade
    public static let typographyFamilyApp: String = "'JetBrainsMono Nerd Font Mono', 'JetBrainsMono Nerd Font', 'JetBrains Mono', 'PlemolJP', 'Noto Sans JP', monospace" // App UI mode — chrome 全般 (sidebar / button / dialog / tab)。等幅 + Nerd Font icon、CJK は PlemolJP fallback で完全等幅統一。VP Phase A1 default
    public static let typographyFamilyRead: String = "'PlemolJP', 'Noto Sans JP', 'JetBrainsMono Nerd Font Mono', monospace" // Read mode — 読み専用表示 (memory view / chat history / canvas markdown render / log viewer)。PlemolJP の Plex Sans JP + JetBrains Mono Latin で CJK 完全等幅統一
    public static let typographyFamilyEditor: String = "'iAWriterDuoS Nerd Font Mono', 'iAWriterDuoS Nerd Font', 'iA Writer Duo S', 'PlemolJP', 'Noto Sans JP', monospace" // Editor mode (write、default) — textarea / Markdown editor / chat input。iA Writer Duo の Duospace で writer 体験、CJK は PlemolJP
    public static let typographyFamilyEditorMono: String = "'iAWriterMonoS', 'iA Writer Mono S', 'PlemolJP', 'Noto Sans JP', monospace" // Editor mode option — 純粋 mono。iA Writer Mono、コード重視 user 向け
    public static let typographyFamilyEditorQuattro: String = "'iAWriterQuattroS', 'iA Writer Quattro S', 'PlemolJP', 'Noto Sans JP', sans-serif" // Editor mode option — semi-proportional。iA Writer Quattro、長文散文 user 向け
    public static let typographyFamilyTerminal: String = "'JetBrainsMono Nerd Font Mono', 'JetBrainsMono Nerd Font', 'JetBrains Mono', 'PlemolJP', 'Noto Sans JP', monospace" // Terminal mode — xterm.js (app と同じ stack だが意味的に分離、将来 terminal 専用調整に拡張可)
}
